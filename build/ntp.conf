

logconfig=syncstatus +sysevents

driftfile /var/lib/ntp/drift
restrict default kod nomodify notrap nopeer noquery
restrict -6 default kod nomodify notrap nopeer noquery
restrict 127.0.0.1
restrict -6 ::1

server ntp.nml.csiro.au         # CSIRO National Measurement Laboratory, Sydney, Australia.
server ntp.per.nml.csiro.au     # Physics Department,University of WA, Perth, Australia.
server ntp.ise.canberra.edu.au  # The University of Canberra, Canberra Australia.

server 0.au.pool.ntp.org        # region releated ntp.org server
server 1.au.pool.ntp.org        # region releated ntp.org server
server 2.au.pool.ntp.org        # region releated ntp.org server
server 3.au.pool.ntp.org        # region releated ntp.org server

server au.pool.ntp.org          # local authority
